# BasicOutputAssignRoleFlow

main: kit: flow: BasicOutputAssignRoleFlow:
{
  entity: "output_assign_role",
  kind: "basic",
  name: "BasicOutputAssignRoleFlow",
  step: [
    {
      input: {
        ref: "output_assign_role_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicOutputAssignRoleFlow"
}