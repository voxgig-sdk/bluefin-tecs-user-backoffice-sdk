# BluefinTecsUserBackoffice SDK

User Back Office Web Service Documentation client, generated from the OpenAPI spec.

Web service for user registration and management.

Learn more about User Back Office Web Service Documentation at [test.tecs.at](https://test.tecs.at).

This is an unofficial SDK for the User Back Office Web Service Documentation public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua SDKs, a CLI, an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as **25 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`load`, `create`):

```ts
const client = new BluefinTecsUserBackofficeSDK()
const outputactivatedigitalmodule = await client.OutputActivateDigitalModule().create({
  response_code: 1,
  response_message: 'example',
})
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = BluefinTecsUserBackofficeSDK.test()
const outputgetlogo = await client.OutputGetLogo().load()
// outputgetlogo is a bare OutputGetLogo populated with mock data
console.log(outputgetlogo)
```

### Python

```python
client = BluefinTecsUserBackofficeSDK.test()
outputgetlogo = client.OutputGetLogo().load()
print(outputgetlogo)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = BluefinTecsUserBackofficeSDK::test([
    "entity" => ["outputgetlogo" => ["test01" => []]],
]);
$outputgetlogo = $client->OutputGetLogo()->load();
```

### Golang

```go
client := sdk.Test()
result, err := client.OutputGetLogo(nil).Load(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = BluefinTecsUserBackofficeSDK.test({
  "entity" => { "outputgetlogo" => { "test01" => {} } },
})
outputgetlogo = client.OutputGetLogo.load()
```

### Lua

```lua
local client = sdk.test()
local result, err = client:OutputGetLogo():load()
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/bluefin-tecs-user-backoffice` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/releases) |
| Python | `voxgig-sdk-bluefin-tecs-user-backoffice` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/releases) |
| PHP | `voxgig-sdk/bluefin-tecs-user-backoffice` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/releases) |
| Golang | `github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go` | `go get github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go@latest` |
| Ruby | `voxgig-sdk-bluefin-tecs-user-backoffice` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/releases) |
| Lua | `voxgig-sdk-bluefin-tecs-user-backoffice` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go-cli` | `go install github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go-cli/cmd/bluefin-tecs-user-backoffice@latest` |
| Go MCP server | `github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go-mcp` | `go get github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { BluefinTecsUserBackofficeSDK } from '@voxgig-sdk/bluefin-tecs-user-backoffice'

const client = new BluefinTecsUserBackofficeSDK({
  apikey: process.env.BLUEFIN_TECS_USER_BACKOFFICE_APIKEY,
})

```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua) | `ts/` `py/` `php/` `go/` `rb/` `lua/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o bluefin-tecs-user-backoffice-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "bluefin-tecs-user-backoffice": {
      "command": "/abs/path/to/bluefin-tecs-user-backoffice-mcp"
    }
  }
}
```

## Entities

The API exposes 25 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **OutputActivateDigitalModule** | The OutputActivateDigitalModule entity (create). | `/activateDigitalModule` |
| **OutputActivatePortalModule** | The OutputActivatePortalModule entity (create). | `/activateMerchantPortalModule` |
| **OutputActivateStoreModule** | The OutputActivateStoreModule entity (create). | `/activateAppStoreModule` |
| **OutputActivateUser** | The OutputActivateUser entity (create). | `/activateUser` |
| **OutputAssignRole** | The OutputAssignRole entity (create). | `/assignRoles` |
| **OutputChangeLogo** | The OutputChangeLogo entity (create). | `/changeLogo` |
| **OutputCreateMandator** | The OutputCreateMandator entity (create). | `/createMandator` |
| **OutputCreateServiceUser** | The OutputCreateServiceUser entity (create). | `/createServiceUser` |
| **OutputDeactivateUser** | The OutputDeactivateUser entity (create). | `/deactivateUser` |
| **OutputGetKycDocument** | The OutputGetKycDocument entity (create). | `/getKycDocument` |
| **OutputGetLogo** | The OutputGetLogo entity (load). | `/getLogo` |
| **OutputListOfAvailableRole** | The OutputListOfAvailableRole entity (create). | `/listOfAvailableRoles` |
| **OutputListOfMandator** | The OutputListOfMandator entity (create). | `/listOfMandators` |
| **OutputListOfModule** | The OutputListOfModule entity (create). | `/listOfModules` |
| **OutputListOfRoleGroup** | The OutputListOfRoleGroup entity (create). | `/listOfRoleGroups` |
| **OutputListOfTransactionsHistory** | The OutputListOfTransactionsHistory entity (create). | `/listOfTransactionsHistory` |
| **OutputListOfUser** | The OutputListOfUser entity (create). | `/listOfUsers` |
| **OutputProvideCredential** | The OutputProvideCredential entity (create). | `/provideCredentials` |
| **OutputRegisterUser** | The OutputRegisterUser entity (create). | `/registerUser` |
| **OutputRemoveRole** | The OutputRemoveRole entity (create). | `/removeRoles` |
| **OutputResendLink** | The OutputResendLink entity (create). | `/resendLink` |
| **OutputResetPassword** | The OutputResetPassword entity (create). | `/resetPassword` |
| **OutputUpdateConsumer** | The OutputUpdateConsumer entity (create). | `/updateConsumer` |
| **OutputUpdateProfile** | The OutputUpdateProfile entity (create). | `/updateProfile` |
| **Version** | The Version entity (load). | `/version` |

The operations available across these entities are **load**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from bluefintecsuserbackoffice_sdk import BluefinTecsUserBackofficeSDK

client = BluefinTecsUserBackofficeSDK({
    "apikey": os.environ.get("BLUEFIN_TECS_USER_BACKOFFICE_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'bluefintecsuserbackoffice_sdk.php';

$client = new BluefinTecsUserBackofficeSDK([
    "apikey" => getenv("BLUEFIN_TECS_USER_BACKOFFICE_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/bluefin-tecs-user-backoffice-sdk/go"

client := sdk.NewBluefinTecsUserBackofficeSDK(map[string]any{
    "apikey": os.Getenv("BLUEFIN_TECS_USER_BACKOFFICE_APIKEY"),
})

```

### Ruby

```ruby
require_relative "BluefinTecsUserBackoffice_sdk"

client = BluefinTecsUserBackofficeSDK.new({
  "apikey" => ENV["BLUEFIN_TECS_USER_BACKOFFICE_APIKEY"],
})

```

### Lua

```lua
local sdk = require("bluefin-tecs-user-backoffice_sdk")

local client = sdk.new({
  apikey = os.getenv("BLUEFIN_TECS_USER_BACKOFFICE_APIKEY"),
})

```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://www.tecs.at/contact/](https://www.tecs.at/contact/)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the User Back Office Web Service Documentation OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
